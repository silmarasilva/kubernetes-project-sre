apiVersion: v1
kind: Pod
metadata: 
  name: pod-clientes
  labels:
    app: pod-clientes
spec:
  containers:
  - name: container-pod-clientes
    image: silmarasilva/apiclientes
    ports:
      - containerPort: 5100
    envFrom:
    - configMapRef:
        name: configmap-cli
